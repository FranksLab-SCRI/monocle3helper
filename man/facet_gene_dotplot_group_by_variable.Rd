% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/facet_gene_dotplot.R
\name{facet_gene_dotplot_group_by_variable}
\alias{facet_gene_dotplot_group_by_variable}
\title{Faceted gene expression dot plot grouped by an arbitrary metadata variable}
\usage{
facet_gene_dotplot_group_by_variable(
  cds,
  markers,
  variable = "Timepoint",
  group_cells_by = "clusters",
  lower_threshold = 0,
  max.size = 10,
  pseudocount = 1,
  scale_max = 3,
  scale_min = -3
)
}
\arguments{
\item{cds}{A \code{SummarizedExperiment}-compatible object containing
single-cell expression data.}

\item{markers}{A character vector of gene IDs or gene short names to plot.}

\item{variable}{A character string specifying the column in
\code{colData(cds)} to use for grouping cells along the x-axis
(for example, \code{"Timepoint"} or \code{"Condition"}).}

\item{group_cells_by}{A character string specifying the column in
\code{colData(cds)} used to group cells along the y-axis.}

\item{lower_threshold}{Numeric value indicating the minimum expression
required for a gene to be considered expressed when calculating the
percentage of cells.}

\item{max.size}{Maximum point size used to scale the percentage of cells
expressing a gene.}

\item{pseudocount}{Numeric pseudocount added prior to log transformation
when computing mean expression.}

\item{scale_max}{Upper bound for clamping mean expression values used for
color scaling.}

\item{scale_min}{Lower bound for clamping mean expression values used for
color scaling.}
}
\value{
A \code{ggplot} object showing faceted dot plots for each gene.
}
\description{
This function generates a faceted dot plot summarizing gene expression
across cell groups and an arbitrary cell-level metadata variable. For each
gene, the mean (log-transformed) expression and the percentage of cells
expressing the gene are computed within each combination of cell group and
metadata category. Dot color represents mean expression, and dot size
represents the fraction of cells expressing the gene.
}
\details{
The function is designed for single-cell expression data stored in a
\code{SummarizedExperiment}-compatible object (for example, a Monocle3
\code{cell_data_set}) and supports flexible grouping by any column in
\code{colData(cds)}.
}
\examples{
\dontrun{
facet_gene_dotplot_group_by_variable(
  cds,
  markers = c("TNNT2", "MYH6"),
  variable = "Timepoint",
  group_cells_by = "clusters"
)
}

}
